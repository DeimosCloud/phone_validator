# /etc/nginx/conf.d/load_balancer.conf

  upstream backend {
    {% for host in groups['backend'] %}
    server {{ hostvars[host]['ansible_host'] }}:32009;
    {% endfor %}
  }

  server {
    listen 80;

    location / {
      proxy_pass http://backend;
    }
  }

